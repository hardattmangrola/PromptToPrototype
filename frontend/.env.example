# Backend API base URL (no trailing slash).
# Leave empty when using Vite proxy: dev server proxies /api to http://localhost:8000
# For production or different host, set e.g. http://localhost:8000
VITE_API_URL=
